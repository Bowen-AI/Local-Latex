% !TEX root = ../main.tex

\section{Multi-File Editing}

This section lives in \texttt{sections/findings.tex}. Because the first line
points back to the root document, the extension can compile the full paper even
when this section file is the active editor.

\begin{quote}
\color{blue!60!black}
The useful demo is not a fancy document. It is a document that proves the
extension can find, build, and preview the thing you are editing.
\end{quote}
